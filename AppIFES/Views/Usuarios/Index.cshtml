@model IEnumerable<AppIFES.Models.Usuario>
@using GridMvc.Html

@{
    ViewBag.Title = "Cadastro de Usuários";
}

<center><h2>Cadastro de Usuários</h2></center>

<p>
    @Html.ActionLink("Novo", "Create", new { area = "" }, new { @class = "btn btn-primary" })
</p>


<div class="table-responsive">
    @*columns.Add(col => col.nome).Titled("Nome").RenderValueAs(col => String.Format("{0}", col.nome)).Sortable(true).Filterable(true).SetWidth(220);
    .SetInitialFilter(GridMvc.Filtering.GridFilterType.StartsWith, "a")*@
    @Html.Grid(Model).Columns(columns =>
{
columns.Add(col => col.idusuario, true);
columns.Add(col => col.nome).Titled("Nome").Sortable(true).Filterable(true);
columns.Add(col => col.email).Titled("Email").Sortable(true);
columns.Add().Encoded(false).Sanitized(false)
.RenderValueAs(col =>
        @<b>
            @Html.ActionLink("Alterar", "Edit", new { Id = col.idusuario }, new { @class = "btn btn-warning" })
            @Html.ActionLink("Detalhar", "Details", new { id = col.idusuario }, new { @class = "btn btn-success" })
            @Html.ActionLink("Apagar", "Delete", new { id = col.idusuario }, new { @class = "btn btn-danger" })
        </b>);

}
    )
</div>